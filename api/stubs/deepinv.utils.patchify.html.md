# patchify

### deepinv.utils.patchify(image, patch_size, stride=None, pad_if_needed=True)

Alias of [`deepinv.utils.image_to_patches()`](https://deepinv.org/api/stubs/deepinv.utils.image_to_patches.html.md#deepinv.utils.image_to_patches). Extracts overlapping 2D patches from images.

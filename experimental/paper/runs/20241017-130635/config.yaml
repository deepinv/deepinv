general:
  name: "structured_shepp_F"
  recon: "spec"
  save: True

signal:
  img_size: 64
  mode: "shepp-logan" # "random", "shepp-logan", "mix", "delta"， "constant"
  config:
    noise_ratio: 0.0 # noise ratio for mix mode
    reverse: False
    varying_norm: False
    permute: False
    max_scale: 40.0 # max scale for varying norm

model:
  n_layers: 0.5
  diagonal:
    mode: 'uniform_phase' # 'uniform_phase', 'gaussian', 'laplace', 'marchenko-pastur', 'student-t', 'uniform', 'triangular'
    config:
      degree_of_freedom: 30
      complex: False
  transform: "fft"
  shared_weights: False

recon:
  series: "arange"
  start: 58
  end: 194
  list: []
  n_repeats: 50
  max_iter: 5000

save:
  path: "../runs"
  name: "res_{model_name}_{img_mode}_{recon}_{oversampling_start}-{oversampling_end}_{n_repeats}repeat.csv"
Project 6 staging payload for a draft PR to `deepinv/deepinv`.

Source payload root:
- `/home/fotis/hackathon/live_work/deepinv_pr_project6_payload`

Contents:
- `bridge/`
  Core wrapper code developed locally for Project 6.
- `examples/`
  Runnable example/demo scripts and comparison script.
- `tests/`
  Smoke tests used locally.
- `submission/`
  Draft PR text and the PDF report.

Suggested destination in a `deepinv/deepinv` checkout:
- `bridge/physics.py`
  adapt into `deepinv/physics/...`
- `bridge/*.py`
  adapt into helper modules near the new physics class
- `examples/*.py`
  adapt into `examples/` or documentation/tutorial materials
- `tests/*.py`
  adapt into `deepinv/tests/...`
- `submission/PR_BODY_DRAFT.md`
  use as PR text

This is a staging bundle, not a ready-to-commit repository layout.
